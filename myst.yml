# See docs at: https://mystmd.org/guide/frontmatter
version: 1

# 项目级别的元数据，适用于整个 Jupyter Book
project:
  # 唯一标识符，用于跨项目引用（可选）
  id: 4da9cb15-177c-41f5-8c4e-6a24b4e87eab
  
  # 项目标题（必填，最大 500 字符）
  title: An example Jupyter Book
  
  # 项目副标题（可选，最大 500 字符）
  subtitle: A practical guide to scientific writing
  
  # 短标题（可选，最大 40 字符，用于导航栏等空间受限处）
  short_title: Jupyter Book Example
  
  # 项目描述（可选，最大 500 字符，用于预览或搜索引擎）
  description: A collection of files that build up a book
  
  # 关键词（可选，用于索引和搜索）
  keywords:
    - jupyter-book
    - something-else
    - scientific-writing
    - data-science
  
  # GitHub 仓库地址（可选，支持 owner/repo 或完整 URL）
  github: captain-jupyter/my-book
  
  # 作者列表（可选，可被页面覆盖）
  authors:
    - name: Zhang Wzy  # 作者姓名（必填）
      orcid: 0000-0001-2345-6789  # ORCID 标识符（可选）
      email: wzy@example.com  # 邮箱（如果是通讯作者则必填）
      corresponding: true  # 是否为通讯作者（默认 false）
      affiliations:
        - id: wanglab  # 引用机构 ID
          institution: Wang Lab University  # 机构名称
          department: Computational Biology  # 部门（可选）
          city: Beijing  # 城市（可选）
          country: China  # 国家（可选）
      roles:  # CRediT 角色（可选）
        - Conceptualization
        - Writing – original draft
  
  # 机构列表（可选，可供作者引用）
  affiliations:
    - id: wanglab
      institution: Wang Lab University
      ror: https://ror.org/03rmrcq20  # 机构 ROR ID（可选）
      address: 123 Science Road  # 地址（可选）
      city: Beijing
      country: China
  
  # 出版日期（可选，格式如 YYYY-MM-DD）
  date: 2025-03-06
  
  # 开源许可（可选，推荐使用 SPDX 标识符）
  license: CC-BY-4.0  # 内容和代码共用许可
  # 或分别指定：
  # license:
  #   content: CC-BY-4.0  # 内容许可
  #   code: MIT  # 代码许可
  
  # 是否开放访问（可选）
  open_access: true
  
  # # DOI（可选，用于已发表作品）
  # doi: 10.5281/zenodo.1234567
  
  # 资助信息（可选）
  funding:
    - statement: This work was supported by Wang Lab Grant.
      id: WL-2025-001
      name: Computational Biology Research Fund
      sources:
        - name: Wang Lab University
      recipients:
        - name: Zhang Wzy
  
  # 标签（可选，用于分类）
  tags:
    - tutorial
    - jupyter
  
  # # 缩略图（可选，用于社交媒体预览）
  # thumbnail: thumbnails/cover.png
  
  # # 横幅图片（可选，用于某些主题）
  # banner: images/banner.jpg
  
  # # 导出配置（可选，用于生成静态文件）
  # exports:
  #   - id: pdf-export
  #     format: pdf  # 支持 pdf, tex, docx, jats 等
  #     template: book  # 模板名称或路径
  #     output: book.pdf  # 输出文件名
  #   - id: html-export
  #     format: md  # Markdown 输出
  #     output: book.md
  
  # # 下载文件（可选，供用户下载）
  # downloads:
  #   - file: assets/data.zip  # 本地文件路径
  #     title: Supplementary Data  # 下载链接显示文本
  #     filename: data.zip  # 下载时的文件名
  
  # 自动生成的目录（运行 `myst init --write-toc` 创建）
  toc:
    - file: intro.md  # 页面文件
    - file: assmbly_singlecell_atlas.ipynb
    # 可添加更多页面：
    # - file: chapter1.md
    # - file: chapter2.md

# 网站级别的配置
site:
  # 网站模板（默认 book-theme 适用于书籍）
  template: book-theme
  
  # # 网站选项（可选）
  # options:
  #   favicon: favicon.ico  # 网站图标
  #   logo: site_logo.png  # 网站标志
  #   logo_text: My Jupyter Book  # 标志旁的文本
  #   show_toc: true  # 是否显示目录
  #   analytics: google-G-XXXXXX  # Google Analytics ID（可选）

  # 导航栏配置（可选）
  nav:
    # - title: Home
    #   url: /
    - title: GitHub
      url: https://github.com/zywu2002/jupyter-book

# # 可扩展其他配置文件（可选）
# extends:
#   - ../common.yml  # 引用其他 YAML 文件